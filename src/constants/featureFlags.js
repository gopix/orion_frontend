// ─────────────────────────────────────────────────────────────
// Feature flags
//
// SHOW_EDITOR_PLUS controls visibility of the Editor+ workspace
// across the whole app: the Home dashboard card, the sidebar nav
// links on other pages, the /editor route, and the post-login
// redirect. Flip it back to `true` once it's ready to be shown
// again (e.g. after a client demo where it should stay hidden).
// ─────────────────────────────────────────────────────────────
export const SHOW_EDITOR_PLUS = true;